{
  "name": "zara-stats-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute zara-stats --local --file=schema.sql",
    "db:remote": "wrangler d1 execute zara-stats --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.141.0"
  }
}
